{
  "name": "@mindlink/shared",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --emitDeclarationOnly --outDir dist || exit 0",
    "type-check": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'shared lint via app'",
    "test": "echo 'no test yet'"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "@mindlink/config": "workspace:*"
  }
}
