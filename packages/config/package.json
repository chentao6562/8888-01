{
  "name": "@mindlink/config",
  "version": "0.0.0",
  "private": true,
  "main": "index.cjs",
  "types": "index.d.ts",
  "files": [
    "eslint-preset.cjs",
    "prettier-preset.cjs",
    "tsconfig.base.json"
  ],
  "scripts": {
    "lint": "echo 'no lint for config'",
    "type-check": "echo 'no type-check for config'",
    "build": "echo 'no build for config'",
    "test": "echo 'no test for config'"
  }
}
